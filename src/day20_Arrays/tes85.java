package day20_Arrays;

import java.util.Arrays;

public class tes85 {
    public static void main(String[] args) {
        {
            /*
                int [][] reverse = new int [array.length][array.length];
                for (int i =0, j = 0;j<array.length&&i<array.length; i++, j++) {
                    for (int b = array[j].length-1, y = 0; b>=0; b--, y++){
                        reverse[i][y]=array[j][b];
                    }
                }System.out.println(Arrays.deepToString(reverse));

           */


        }
    }
}
